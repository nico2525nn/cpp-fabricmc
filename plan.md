# C++によるMinecraftサーバー実装cpp-fabricmcの本家server.jar代替化戦略：11分野の網羅的分析とアーキテクチャ設計

本レポートは、オープンソースのC++によるMinecraftサーバー実装である`cpp-fabricmc` [[1,2]] が、公式のJava版`server.jar`と比較して存在する機能ギャップを網羅的に分析し、その解消に向けた体系的かつ詳細な技術的改善計画を提示することを目的とする。ユーザーの要求として、「どれだけ長くなっても良い」「時間がかかっても良い」という無制限の時間的余裕が許容されていることから、単なるバグ修正や軽微な機能追加に留まらず、`cpp-fabricmc`を完全な代替品として位置づけるための、モジュール構成、データフロー、状態管理、イベントハンドリングを含む包括的なアーキテクチャ設計を提供することが必須である <user_request>。特に、インベントリ操作の反映、ブロック更新伝播、MobのAIと永続化、Brigadierベースのコマンドシステムといった高影響度項目に深く焦点を当てる <user_request>。本レポートは、現在の開発チームだけでなく、将来的なコントリビューターも想定し、技術的背景の有無にかかわらず理解可能な完全性を目指す。

## ワールド生成と地形の再現：データ駆動型設計による形態学からの脱却

`cpp-fabricmc`の現状において、最も顕著なギャップはワールド生成領域にある <user_conversation>。地形形状、バイオーム配置、構造物、鉱石分布など、全ての要素が本家と大きく異なる。この差異の根本原因は、地形生成の背後にあるアルゴリズムの違いにある。本家では、地形の密度、表面の形状、バイオームの選択、構造物の配置といった一連のプロセスが、全てJSONファイルで定義された複雑なノイズ関数と条件式によって駆動されている [[148,203]]。これに対し、`cpp-fabricmc`は独自のPerlinオクターブ近似のような簡易的なノイズ関数を使用している可能性が高い <user_conversation>。このため、同一のシードでも地形が全く異なる結果となり、探索要素が失われている <user_conversation>。本章では、これらのギャップを解消するための、データ駆動型のアーキテクチャ設計を提案する。

まず、地形生成の核となる「密度関数パイプライン」の再現から始める。本家の地形生成は、`overworld.json`といったJSONファイルで定義された巨大な木構造を持つ密度関数グラフによって制御されている [[203]]。このグラフは、複数のノイズ源（例：base_3d_noise, factor, jaggedness）を加算、減算、スケーリング、クリッピングといった数学的な演算を通じて組み合わせることで、最終的な地形の密度値を計算する [[203]]。例えば、「sloped_cheese」密度関数は、大陸性、侵食、山脈と谷のノイズを組み合わせて表面の高さと形状を決定し、`depth`密度関数はY座標の勾配とノイズを足して一般の地表高を決定する [[203]]。`cpp-fabricmc`がこれを実装するには、以下の3つの技術的ステップが必要となる。

第一に、**データ駆動型のアーキテクチャ設計**である。`density_function`ディレクトリ以下に配置されるJSONファイルを読み込み、それをC++で実行可能なオブジェクトに変換するためのVM（バーチャルマシン）またはインタプリタを構築する必要がある。これは、Java版の`net.minecraft.world.level.levelgen.DensityFunction`クラス群の役割を模倣するものである。このアーキテクチャでは、`DensityFunctionRegistry`という静的クラスが登録済みの関数タイプ（例：`minecraft:add`, `minecraft:noise`）を識別し、`DensityFunctionParser`がJSONの構文を解析して対応する`DensityFunction`派生クラスのインスタンスを生成する。各`DensityFunction`クラスは、`evaluate(StructureWorldAccess world, double x, double y, double z)`といった共通インターフェースを持ち、再帰的に子ノードの評価を呼び出すことで、最終的な密度値を計算する。この設計により、新しい密度関数の追加や既存の関数の修正が、コードの再コンパイルなしにJSONファイルの編集だけで可能になる。

第二に、**性能を考慮した実装手法の選択**である。純粋にJSONを毎回パースするのではなく、一度読み込んだJSONをパースし、内部で効率的なデータ構造（例えば、ポインタで結ばれた計算グラフ）に変換するプロセスを導入する。さらに、開発初期段階での開発効率を高めるために、PythonやLuaなどのスクリプト言語を使ってJSONを解析し、C++用のシリアライズ済みバイナリデータ（例：`.dfc`ファイル）を出力するツールチェーンを開発することも有効である。このバイナリデータは、ランタイムでのパースコストを大幅に削減できる。ただし、最終的にはJSON形式を直接サポートするロジックを実装し、データパックによるカスタム生成の柔軟性を維持する必要がある。

第三に、**依存する他のノイズ関数との統合**である。密度関数は単独で動作するのではなく、大陸性、湿度、侵食、浸食、深さ、奇妙さといった6つの主要なパラメータを元に動作する [[148]]。これらはそれぞれ別のノイズマップから生成され、密度関数内で`minecraft:noise`関数によってアクセスされる。そのため、`cpp-fabricmc`は、これらの基本的な6つのパラメータを計算するためのノイズ生成器（例：Perlinノイズ、Simplexノイズの実装）も併せて実装する必要がある。これにより、密度関数が期待通りの値を受け取り、地形の広域的な特徴（例：大陸、海岸線、山脈）を正しく表現できるようになる。

次に、**バイオーム配置**の再現である。本家では、最終的な地形の密度値や温度、湿度、大陸性といったパラメータを元に、`MultiNoiseBiomeSource`がどのバイオームがその座標に割り当てられるかを決定する [[148]]。`cpp-fabricmc`は、上記で実装した密度関数の評価結果を`BiomeSource`クラスに渡し、`getBiomes()`メソッド内でMojoの`MultiNoiseBiomeSource`の振る舞いを模倣するロジックを実装する。このロジックは、定義済みのバイオーム候補の中から、各座標の6軸のパラメータ値が収まる範囲のものを選択する。この際、`BiomeModifications` APIのような、データパックでバイオームの特性を変更したり、新しい特徴を追加したりするためのAPIを提供することが望ましい [[83]]。

構造物の生成も同様に、データ駆動型で実装すべき重要な要素である。本家では、村、廃墟、遺跡、ダンジョンといった多数の構造物が、`configured_structure_feature`と`placed_feature`という二段階のJSON定義で管理されており、生成ステップ（例：`STRUCTURE_START`, `FEATURE`）に従って生成処理が行われる [[148]]。`cpp-fabricmc`は、この仕組みを再現するために、`Feature`と`PlacedFeature`という抽象基底クラスを定義する。`Feature`は、`generate()`という仮想関数を持ち、その派生クラス（`StructureFeature`, `TreeFeature`など）で具体的な生成ロジックを実装する。一方`PlacedFeature`は、特定の`Feature`のインスタンスと、その生成条件（例：出現回数、高さ制限、特定のブロック上のみ生成）を保持する。`GenerationStep`列挙型に対応するハンドラーを用意し、各ステップで対応する`PlacedFeature`のリストを走査して生成処理を呼び出す。構造物自体は、`structure_piece`のJSON定義をパースし、`StructureComponent`の派生クラスとして実装された部品の集合として扱うことで、複雑な建物も正確に再現できる [[148]]。

最後に、**鉱石分布**の再現である。本家では、`ore_vein_type`という定義されたパターンに従って鉱石が生成され、それが`CountPlacement`や`HeightRangePlacement`といった配置modifierによって出現頻度や高さ帯が調整される [[148]]。`cpp-fabricmc`は、この`PlacementModifierType`の概念をC++で再現する。`PlacementModifier`というインターフェースを定義し、`CountPlacementModifier`, `InSquarePlacementModifier`などの具体的な実装を作成する。これにより、`PlacedFeature`の生成ロジック内で、順番にこれらのmodifierを適用することで、煤や鉄鉱石が洞窟内や地上でどのように分布するかといった複雑な挙動を表現できる。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **密度関数パイプライン** | 独自Perlinオクターブ近似 [[49]] | JSON定義の密度関数グラフをパースし実行するVM/インタプリタ [[203]] | `DensityFunctionRegistry`, `DensityFunctionParser`, 各`DensityFunction`派生クラスの実装 |
| **バイオーム配置** | 単一biome(plains固定) [[39]] | MultiNoiseBiomeSourceの振る舞いを模倣し、JSON定義に基づいて配置 [[148]] | `BiomeSource`クラスの実装、`BiomeModifications` API準拠の拡張機構 |
| **構造物生成** | なし | 各種構造物をステップベースでJSON定義に従って生成 [[148]] | `Feature`/`PlacedFeature`の設計、`StructureComponent`の実装、ステップごとのハンドラー登録 |
| **鉱石分布** | 単純ノイズ閾値 [[49]] | `ore_vein_type` + `PlacementModifierType`で構成される多様な分布を表現 [[148]] | `PlacementModifier`インターフェースとその実装の導入 |

これらの改善を段階的に実装することで、`cpp-fabricmc`は単なる幾何学的な地形から、生物群落の多様性、探索の楽しみ、資源採取の戦略性を備えた本格的なMinecraftの世界へと進化していく。これは、あらゆる上位レイヤーのゲームプレイが成立するための不可欠な土台となる。

## 物理法則と世界のダイナミクス：イベント駆動型アーキテクチャによる再構築

`cpp-fabricmc`の現状では、世界の物理法則が極めて単純化されており、大きな機能ギャップとなっている <user_conversation>。ブロックの設置以外に変化がなく、水が流れない、砂が落下しない、レッドストーンが動かないといった問題は、単なる未実装機能の問題ではなく、システムの根本的な設計思想の違いに起因している。本家Minecraftは、あるブロックの状態が変化したときに、その変化が隣接するブロックに伝播するという「更新伝播」の仕組みを基盤としている [[194]]。本章では、このイベント駆動型のアーキテクチャを採用し、水の流れ、砂の落下、明るさの伝播といったダイナミックな世界の物理法則を`cpp-fabricmc`に再構築するための詳細な技術的改善方針を提示する。

第一に、**ブロック更新伝播システムの設計**である。これは、全てのダイナミックなブロック挙動を実現するための基盤となる。本家の仕組みでは、あるブロックが更新されると（例：水が湧き出る、砂が倒れる）、`AbstractBlock.neighborUpdate`メソッドが呼び出され、そのブロック自身が隣接するブロックに対して、自分自身の更新が起こったことを通知する [[281]]。この通知を受け取った隣接ブロックは、自身の状態を更新する必要があるかどうかを判定し、必要であればさらにその隣接ブロックに通知を伝播させる [[194]]。`cpp-fabricmc`がこれを実装するには、以下のようなアーキテクチャを構築する必要がある。

*   **`BlockState`と`BlockBehavior`の分離:** `Block`クラスを抽象化し、ブロックの静的な特性（名前、硬度など）と、動的な振る舞い（落下、浸透など）を分離する。`BlockBehavior`という抽象基底クラスを定義し、`FallingBlockBehavior`, `WaterloggableBehavior`といった具象クラスを用意する。これにより、各ブロックが持つ振る舞いを独立して管理・拡張できる。
*   **`BlockUpdateEvent`の定義:** 更新が発生した際の情報を保持するためのイベントクラス`BlockUpdateEvent`を定義する。このイベントには、更新が発生した座標、更新元となったブロック情報などが含まれる。
*   **更新キューの導入:** 各チックごとに処理される`World::tick()`関数内で、更新キューというキュー型のデータ構造を用意する。更新が発生した際には、その座標とブロック情報をこのキューに積む。その後、チック終了までにキューに積まれたすべての更新について、順次処理を行う。これにより、同時に多数の更新が発生しても安定して処理を進めることができる。更新の処理は、まず`BlockUpdateEvent`をイベントシステムに発行し、それに反応した各種の`BlockBehavior`が更新を処理するという形にする。

第二に、**水の流れの再現**である。本家では、流れる水は単一のブロック状態（`flowing_water`）として存在するのではなく、個々のエンティティとして扱われている [[76]]。このエンティティは、毎チック重力を受けて下方に移動し、支持体がない場合に限り水平方向にも動き、一定条件下で静止水（`water`）に変化する [[77,79]]。`cpp-fabricmc`は、この概念を`FallingBlockEntity`という独立したエンティティクラスとして実装する。このエンティティは、`update()`関数内で重力を適用し、周囲のブロック状態をチェックして落下または水平移動を判定する。また、`onLand()`などのコールバック関数を用意し、地面に着地した際に、自身の座標のブロックを静止水に変更するロジックを含める。これにより、水が自然に低い方へ流れていき、湖を形成するダイナミックな挙動が再現される。さらに、`WaterBlock`が更新された際にこのエンティティを生成するロジックを追加すれば、水の湧出といったより複雑な挙動も可能になる [[196]]。

第三に、**砂・砂利の落下**である。これは上記の`FallingBlockEntity`の概念を流用することで容易に実装できる。砂や砂利は、`GravityAffectedBlock`のようなフラグを持つブロックとして定義され、その下のブロックがなくなる（空気や液体）と、`FallingBlockEntity`が生成される。このエンティティは、`update()`関数内で重力を継続的に適用し、落下速度を増していく。地面に着地した瞬間、エンティティは削除され、その座標に着地した結果として生まれるブロック（例：砂は砂岩に、砂利は普通の岩に）を設置する [[176,177]]。この実装により、サンドキャンドルやサンドウォールの崩壊、サンドデューパーといったレッドストーン回路の一部として利用される落下現象が正確に再現される。

第四に、**レッドストーンの完全シミュレーション**である。レッドストーンは、信号の伝播、強度の減衰、パルスの生成といった複雑な論理回路を構成するためのシステムであり、それは全てブロック更新の伝播によって駆動されている [[209,248]]。例えば、赤石粉が更新されると、その隣の赤石粉や赤石タルタルに更新が伝わり、信号が伝播する。赤石TORCHが更新されると、その出力がON/OFFし、接続された機械を制御する。`cpp-fabricmc`は、上記で設計した更新伝播システムを基盤とすることで、レッドストーンの全ての振る舞いを再現できる。具体的には、赤石粉、赤石TORCH、赤石レバー、ピンボールなど、全ての赤石関連ブロックに対して`neighborUpdate`のロジックを実装し、信号の伝播と強度の計算を行う。例えば、`RedstoneWireBlock`の`getStateForNeighborUpdate`は、接続先の赤石粉に信号を伝播させる更新を発行する。また、`Observer`のような、特定の更新を検知してパルスを発生させる特殊なブロックも実装する必要がある [[297,298]]。これにより、単純な動かないレッドストーンから、複雑な時計回路やメモリ回路まで、ほぼ全てのレッドストーン装置が動くようになる。

第五に、**明るさシステムの再現**である。本家では、光源（例：松明、ダイヤモンド）が設置されると、そのブロック自体が`block light`を放出し、それが周囲に伝播していく。また、空からの光である`sky light`も、雲の有無や垂直方向の距離に応じて計算される。この両方が合成され、各ブロックの明るさが決定される [[256]]。`cpp-fabricmc`は、このシステムを`LightEngine`という独立したモジュールとして実装する。このエンジンは、`SkyLightStorage`と`BlockLightStorage`という2つのChunkDataレイヤーを持つ。光源ブロックが設置または削除されると、`LightEngine`はその周囲のライト値を更新する「エッジ」をキューに積み、BFS（幅優先探索）で伝播させていく。これにより、松明の周りに影がつき、洞窟の奥行きが表現されるリアルな照明が実現される。`level.dat`に保存される自動保存設定は、本家よりも高い頻度（3秒毎）で実装されているが、これは望ましい改善点である 。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **ブロック更新伝播** | なし(単一ブロックのみ変更) [[194]] | 更新が隣接ブロックにイベント駆動で伝播 [[193]] | `BlockUpdateEvent`、更新キュー、`neighborUpdate`コールバックの実装 |
| **水の流れ** | なし(静的) [[76]] | `FallingBlockEntity`を用いたエンティティとしての水流 [[77]] | `FallingBlockEntity`クラスの実装、重力と衝突判定の追加 |
| **砂・砂利落下** | なし [[176]] | `FallingBlockEntity`を用いた落下と着地後のブロック変化 [[177]] | 上記`FallingBlockEntity`の流用、`onLand`コールバックの実装 |
| **レッドストーン** | なし [[209]] | 更新伝播を基盤とした信号の完全なシミュレーション [[248]] | 各レッドストーンブロックでの`neighborUpdate`ロジックの実装 |
| **明るさシステム** | sky light近似、block light未実装 [[256]] | Sky LightとBlock Lightの両方を伝播計算 [[256]] | `LightEngine`モジュール、`SkyLightStorage`/`BlockLightStorage`の実装、BFS伝播ロジック |

これらの物理法則の再構築は、`cpp-fabricmc`を単なる建築プラットフォームから、プレイヤーの創造性が試されるダイナミックなsandboxへと昇華させる上で不可欠なステップである。

## エンティティとMobの復活：永続化とAIを支えるエンティティシステムの設計

`cpp-fabricmc`の現状では、Mobはゾンビや動物といった5種類に限定され、高度なAIや永続化といった本家に見られる重要な要素が欠落している <user_conversation>。Mobは単一の行動しかせず、仲間やプレイヤーとの相互作用もなし、サーバーを再起動すれば全て消えてしまう。これは、エンティティシステムが非常に単純化されているためである。本章では、MobのAI、行動セット、永続化、そしてMob間の相互作用を実現するための包括的なエンティティシステムのアーキテクチャ設計を詳細に提示する。

第一に、**MobのAIと行動セット（ゴールシステム）の実装**である。本家MinecraftのMobは、単一の行動ではなく、攻撃、逃走、歩き回る、餌を与えるといった複数の「目標」を持ち、それらを優先度に基づいて切り替えながら行動する [[19]]。この「脳」の概念を`cpp-fabricmc`に実装するには、以下の3つの主要なクラスを設計する。

*   **`Goal`（目標）抽象基底クラス:** `Goal`は、`canStart()`, `shouldContinue()`, `start()`, `tick()`, `stop()`といった純粋仮想関数を持つ抽象クラスである。これにより、各行動（例：`MeleeAttackGoal`, `WanderGoal`, `MateGoal`）が独立したクラスとして実装でき、交換可能になる。
*   **`Brain`（脳）クラス:** `Brain`は、現在実行中の`Goal`を管理する役割を持つ。`update()`関数内で、登録されている全ての`Goal`の`canStart()`を評価し、成功した最初の`Goal`を選択して実行を開始する。もし現在実行中の`Goal`の`shouldContinue()`が偽を返せば、`stop()`を呼び出し、次のチックで再度`canStart()`の評価を再開する。これにより、行動の優先順位付けと状態遷移が実現される。
*   **`Pathfinder`（経路探索）:** Mobが目的地まで移動するためには、A*アルゴリズムを用いた経路探索が必要である。`Pathfinder`クラスを実装し、`findPath(Entity from, BlockPos to)`といった関数を提供する。このクラスは、ブロックの通過ペナルティ（例：水中ではペナルティが高い）を考慮し、Mobが自然な動きで目的地まで到達するための経路を計算する [[27]]。

第二に、**エンティティの永続化**である。現在の`cpp-fabricmc`では、Mobのデータはサーバー再起動時に全て破棄される 。これを解決するためには、全てのエンティティ、特にMobとプレイヤーのデータをRegionファイルに定期的に書き込む仕組みが必要である。このためのアーキテクチャは以下の通りである。

*   **エンティティシリアル化:** 各`Entity`の派生クラス（`PlayerEntity`, `ZombieEntity`, `CowEntity`など）に、`serialize(NbtCompound& nbt)`と`deserialize(const NbtCompound& nbt)`という2つの関数を実装する。この関数群は、エンティティ固有のデータ（例：HP、飢餓度、持ち物、Mobのアイデンティティなど）をNBT（ネイティブバイナリタグ）形式のデータ構造にエンコーディングし、デコーディングする役割を持つ。
*   **永続化サービスの実装:** `EntityPersistenceService`のような静的サービスクラスを作成する。このサービスは、`Server::saveAll()`関数で呼び出される。この関数は、存在する全てのエンティティを走査し、`PlayerEntity`の場合は`playerdata`ディレクトリに、`MobEntity`の場合は`entities`ディレクトリに、エンティティIDをファイル名とした`.dat`ファイルにシリアル化したデータを書き込むロジックを持つ。これにより、サーバー停止時や自動保存タイマーが期限切れになった際に、エンティティの状態が安全に保存される [[136]]。

第三に、**Mob間の戦闘と相互作用**の実装である。動物がゾンビから逃げたり、村人たちが職業ブロックで働いたりするためには、Mob間の関係性と、それがAIに与える影響を定義する必要がある。

*   **敵対関係の定義:** `EntityType`に`SpawnGroup`（例：CREATURE, MONSTER, AMBIENT, WATER_CREATURE）という列挙型を関連付ける。そして、`EntityType`同士の敵対関係を判定する静的な関数`isHostileTo(EntityType a, EntityType b)`を実装する。例えば、`SpawnGroup::MONSTER`のMobは`SpawnGroup::CREATURE`のMobに対して常に敵対的であるとする。
*   **AIとの連携:** `Brain`内の`checkForNearestEnemy()`という`Goal`を実装する。この`Goal`は、`isHostileTo()`関数を呼び出して、周囲にいるMobが自分にとって敵対的かどうかを確認する。もしプレイヤーや仲間の敵対Mobを見つけたら、攻撃や回避といった行動を開始する。これにより、ゾンビがプレイヤーを襲う、狼がゾンビを攻撃するといった自然な生態系が再現される。

第四に、**Mobの多様性と種族の拡張**である。現在の5種類から74種類以上に増やすには、Mobの定義をデータ駆動で行う仕組みが必要である。

*   **データ駆動のMob定義:** Mojangの`entity_type`のJSON定義を参考に、`cpp-fabricmc`でも同様のJSONファイルでMobの定義を行う。このJSONには、Mobの表示名、エンティティクラス名、最大HP、被ダメージ量、スポーン条件、レシピ（繁殖素材）などが記述される。
*   **MobRegistryの導入:** `MobRegistry`という静的クラスを用意し、起動時に`entity_type`ディレクトリ配下のJSONファイルを読み込んで、エンティティタイプのIDとその定義をマッピングする。これにより、Modのように新たなMobを追加することができる。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **Mob AI** | 単純wander/zombie chase のみ [[19]] | Priority-based Goal System + Pathfinding [[19]] | `Goal`抽象クラス、`Brain`クラス、A*アルゴリズムの実装 |
| **Mob間戦闘** | なし | EntityTypeごとに敵対関係が定義され、AIが反応 [[44]] | `SpawnGroup`の定義、`isHostileTo()`関数、`Brain`との連携 |
| **エンティティNBT永続化** | **Mobはメモリ上のみ** [[90]] | Region fileにエンティティデータを保存 [[90]] | `serialize`/`deserialize`メソッド、`EntityPersistenceService`の実装 |
| **Mob種類** | 5種類(pig/cow/sheep/chicken/zombie) | 74種類以上、JSON定義による拡張 | `EntityType`のJSON定義、`MobRegistry`の実装 |
| **繁殖** | なし | 餌付け→子産み [[169]] | `MateGoal`の実装、JSONでの繁殖素材定義 |
| **装備** | なし | 手持ち/防具/ドロップ [[169]] | `MobEntity`に`EquipmentComponent`を追加、ドロップロジックの実装 |

これらの改善を実施することで、`cpp-fabricmc`の世界は生命に満ちた、予測不能で魅力的な環境へと変貌する。Mobは単なる障害物ではなく、生態系の一員としてプレイヤーの冒険を彩る存在となる。

## ゲームプレイの基盤：インベントリ、クラフト、およびコマンドシステムの一体化

`cpp-fabricmc`の現状では、高度なゲームプレイの基盤となるシステムがほとんど実装されていない <user_conversation>。インベントリ操作がサーバーに反映されず、アイテムを移動・クラフトできない、そしてコマンドは`/help`や`/ping`程度しか使えない。これは、サーバーとしての機能が極端に不十分であることを意味する。本章では、これら三つの核心的なシステム——インベントリ操作の同期、クラフトロジックの実装、そしてBrigadierベースのコマンドシステムの再現——を一体化したアーキテクチャで設計し、`cpp-fabricmc`を本格的なマルチプレイヤーゲーム環境へと昇華させるための技術的改善方針を詳述する。

第一に、**インベントリ操作の反映とUIの同期**である。現在、`cpp-fabricmc`はクライアントが送信する`WindowClickC2S`パケットを受信した後、何も処理せずに破棄している <user_conversation>。これでは、プレイヤーがドラッグ＆ドロップやシフト＋クリックでアイテムを移動させても、サーバー上のインベントリとクライアント上のUIが完全にデシンクロナイズされ、ゲームプレイが不可能になる [[13,17]]。この問題を解決するためには、クライアントからの操作を正確に解釈し、サーバーの状態を更新した上で、その結果をクライアントに通知する双方向の通信フローが必要である。

*   **Packet Handlerの実装:** `WindowClickC2S`パケットを受信するハンドラー関数を実装する。この関数は、パケット内のウィンドウID、スロット番号、マウスボタン、操作ID、アイテムスタックなどを正確に解析する。
*   **InventoryManagerの導入:** 各エンティティ（プレイヤー、チェスト、かまどなど）が持つインベントリを表す`InventoryComponent`というコンポーネントを定義する。このコンポーネントは、アイテムスタックの配列（例：`std::array<ItemStack, 36>`）を内部に保持し、アイテムの追加、削除、置き換えといった基本操作を提供する。
*   **操作のロジック分離:** 解析したパケットの内容に応じて、`InventoryManager::handleSlotClick()`や`InventoryManager::handleDragDrop()`といった関数を呼び出し、対応するロジックを実行する。例えば、シフトクリックは自動的にインベントリの空いているスロットにアイテムを移動させるロジックになり、ドラッグ＆ドロップはウィンドウ間でのアイテム移動を表す。
*   **UIの同期:** インベントリの操作がサーバー上で完了すると、直ちにその結果をクライアントに通知する。`ContainerSetContentS2C`パケットでウィンドウ内の全アイテムを再送信し、`ContainerSetSlotS2C`パケットで変更された特定のスロットのみを更新する。これにより、クライアントのUIがサーバー上の状態とリアルタイムで同期される [[174]]。

第二に、**クラフトとコンテナUIの実装**である。工作台や醸造台といった拡張UIを開き、そこに置かれたアイテムを使ってクラフトする機能を追加する。

*   **コンテナUIの実装:** チェスト、工作台、かまど、醸造台など、複数のスロットを持つコンテナは、全て同じ`Container`クラス（またはインターフェース）を継承するように設計する。`Container`は、スロット数やUIのタイトルを定義する。プレイヤーが右クリックした際、該当する`BlockEntity`（チェストなど）がサーバーに存在するかを確認し、存在すれば対応する`Container`インスタンスを作成し、クライアントに`OpenContainerS2C`パケットを送信する。これにより、クライアントは対応するGUI（ユーザーインターフェース）を開くことができる [[128]]。
*   **Recipe RegistryとCraftingManager:** `recipes`ディレクトリ配下のJSONファイルを読み込み、`Recipe`という抽象基底クラスを実装する。`ShapedRecipe`, `ShapelessRecipe`といった具体的なレシピクラスを用意する。これらを登録する`RecipeRegistry`と、`craftItem(Inventory input, Recipe recipe)`といった関数でクラフト結果を計算する`CraftingManager`を実装する。これにより、JSONで定義されたあらゆるクラフトパターンがサーバー上で認識され、実行可能になる [[116]]。
*   **UIとの連携:** クライアント側でクラフトボタンが押された際、クライアントは使用されたレシピの情報をサーバーに送信する。サーバー側の`CraftingManager`がそのレシピを検索し、入力インベントリから必要な素材を消費して、結果のアイテムを出力インベントリに追加する。この結果をクライアントに通知し、UIを更新する。

第三に、**Brigadierベースのコマンドシステムの再現**である。本家Minecraftのコマンドシステムは、Mojangが開発したBrigadierライブラリによって支えられており、複雑な構文、引数の検証、タブ補完、そして@aや@pといったターゲットセレクターを提供する [[29]]。`cpp-fabricmc`は、このBrigadierの振る舞いをC++で再現することで、本格的なサーバー管理者ツールを提供できる。

*   **ArgumentBuilderの移植:** Brigadierの構文木を構築するための`CommandNode`とその派生クラス`LiteralArgumentNode`, `RequiredArgumentNode`をC++で実装する。これにより、`dispatcher.register(literal("give").then(argument("targets", players()).build()))`といった構文で、コマンドツリーをプログラムで構築できる [[133]]。
*   **CommandDispatcherとExecutor Pattern:** `CommandDispatcher`クラスを実装し、登録されたコマンドツリーを走査するロジックを内包する。入力された文字列をBrigadierのアルゴリズムに従って解析し、対応する`CommandNode`にマッチするかを判定する。マッチした場合、そのノードが関連付けられた`CommandExecutor`（コマンド実行者のこと）の`execute()`メソッドが呼び出される [[33]]。
*   **標準引数型の実装:** `StringArgumentType`, `IntegerArgumentType`, `FloatArgumentType`, `PlayerArgumentType`といった標準の引数型を実装し、タブ補完のための`Suggestions`機能も提供する。これにより、ユーザーがコマンドを入力する際に、有効な引数候補を自動的に表示できる。
*   **Target Selectorのサポート:** `@a`, `@p`, `@e`, `@r`といったターゲットセレクターを解析するロジックを追加する。入力されたセレクターを`EntitySelector`クラスに渡すと、その条件に合うエンティティのリストを返す。これにより、特定のプレイヤーだけをターゲットにするなど、高度なコマンドが可能になる [[138,142]]。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **インベントリ操作** | 受信するが解析せず破棄 <user_conversation> | WindowClickPacketを完全に処理し、UI状態を同期 [[17]] | `WindowClickC2S`ハンドラー、`InventoryManager`、`Container`の実装 |
| **クラフト** | なし <user_conversation> | 3x3/2x2/工作台でCraftingTableRecipeが使用 [[116]] | `Recipe`/`CraftingManager`の実装、JSONレシピのロード |
| **拡張UI** | 開かない <user_conversation> | チェスト/かまど/作業台等のコンテナUIが開ける | `BlockEntity`（チェスト等）の実装、`Container`の実装、`OpenContainerS2C`の送信 |
| **Brigadier tree** | `/help` `/ping` のみ宣言 <user_conversation> | 完全なコマンドツリーを動的に構築 [[29]] | `CommandDispatcher`、`ArgumentBuilder`、`CommandExecutor`の実装 |
| **引数型** | 文字列分割のみ <user_conversation> | 全brigadier引数型（Integer, Playerなど）をサポート [[133]] | 各種`ArgumentType`の実装、`Suggestions`機能の追加 |
| **セレクター** | なし <user_conversation> | `@a`, `@p`, `@e`... を完全にサポート [[138]] | `EntitySelector`クラスの実装、セレクター文字列の解析ロジック |

これらのシステムを一体的に実装することは、`cpp-fabricmc`を単なる「遊びつくせるオープンワールド」から、「本家サーバーと同等の機能を備えた、創造的かつ戦略的なゲーム環境」へと引き上げる上で決定的な一歩となる。

## 永続化と環境設定：サーバー再起動後の記憶と世界のルール

`cpp-fabricmc`が本家`server.jar`の代替として機能するためには、サーバーの状態を再起動しても保持され、プレイヤーが自分の進捗を失わないような堅牢な永続化機構が不可欠である <user_conversation>。現在、`cpp-fabricmc`は`level.dat`や`playerdata`/*.datといった主要な永続化ファイルの書き出しを行っておらず、これによりゲームルールの変更や難易度、プレイヤーのスポーン地点、インベントリ、健康状態などが全てリセットされてしまう 。また、ゲームルールを動的に変更する`/gamerule`コマンドも実装されていない 。本章では、これらの永続化と環境設定の問題を解決するための、データコンテナ、リポジトリパターン、そしてゲームルールシステムの包括的なアーキテクチャ設計を提示する。

第一に、**サーバー全体の設定と統計情報を保持する`level.dat`の実装**である。本家では、このファイルがサーバーの基本的な設定を保持する。`cpp-fabricmc`は、この振る舞いを再現するための`LevelData`クラスを設計する。

*   **`LevelData`データコンテナの設計:** `LevelData`というC++クラスを定義し、メンバ変数として以下の情報を保持する。
    *   **ゲームルール (`GameRules`):** `doDaylightCycle`, `pvp`, `difficulty`など、全36のゲームルールの値を保持する [[42]]。これは、キーがゲームルール名、値がその設定値（真偽値、整数など）となるマップコンテナで実装される。
    *   **世界情報:** スポーンポイント、ワールドの作成日時、最終更新日時、統計情報の更新日時など。
    *   **統計情報 (`Statistics`):** プレイヤーが行った様々な行動のカウント（例：歩いた距離、アイテムを集めた回数など）を保持する [[136]]。
*   **`LevelDataManager`リポジトリの実装:** `LevelDataManager`という静的サービスクラスを作成する。このクラスは、サーバー起動時に`world/level.dat`ファイルを読み込み、`LevelData`オブジェクトをデシリアライズして保持する。また、`autosave`タイマー（本家は5分毎だが、cpp-fabricmcは3秒毎に設定されているが、これは望ましい改善点である ）が期限切れになったり、サーバーがシャットダウンする際に、`LevelData`オブジェクトをシリアル化し、`level.dat`に上書き保存するロジックを持つ。これにより、サーバー再起動後も全ての世界設定や統計情報が復元される。

第二に、**各プレイヤーの個別データを保持する`playerdata`ディレクトリの実装**である。プレイヤーの位置、健康状態、インベントリ、レベル、XPなど、プレイヤー固有の情報は`playerdata`ディレクトリ配下の個別の`.dat`ファイルに保存される [[136]]。

*   **`PlayerData`データコンテナの設計:** `PlayerData`というC++クラスを定義し、`UUID`をキーとしてインスタンスが管理される。このクラスには、プレイヤーの`Position`, `Inventory`, `Health`, `FoodTick`, `ExperiencеPoints`, `Level`など、プレイヤーの状態を表す全てのデータを保持する。
*   **`PlayerDataManager`リポジトリの実装:** `PlayerDataManager`という静的サービスクラスを作成する。このクラスは、`PlayerEntity`がログアウトした時や、一定時間経過時などに、対応する`PlayerData`オブジェクトを`playerdata`ディレクトリに保存するロジックを持つ。ログイン時には、そのプレイヤーのUUIDに対応する`.dat`ファイルを読み込み、`PlayerData`を復元し、`PlayerEntity`の初期状態として適用する。これにより、プレイヤーがサーバーを離れても、その進行状況が保持され、再ログイン時に以前の状態から続きが可能になる。

第三に、**動的に変更可能なゲームルールシステムの実装**である。本家では、`/gamerule`コマンドで`doFireTick`, `sendCommandFeedback`, `maxEntityCramming`といった多数のゲームルールをオン/オフまたは数値で変更できる [[43,44]]。`cpp-fabricmc`は、この振る舞いをBrigadierコマンドと永続化機構の連携で実現する。

*   **`GameRule`クラスの実装:** `GameRule<T>`というテンプレートクラスを定義し、`bool`, `int`, `float`といった異なる型のゲームルールを扱えるようにする。このクラスは、ルールのデフォルト値、現在の値、そして値のバリデーションロジックを保持する。
*   **`GameRuleManager`の実装:** `GameRuleManager`という静的サービスクラスを作成する。このクラスは、`LevelData`内の`GameRules`マップを操作する。`setGameRule(ruleName, value)`関数は、指定されたルール名の`GameRule`オブジェクトの値を変更し、それが有効な値かを検証する。変更が成功したら、`LevelData`の変更フラグを立て、次回の自動保存時に`level.dat`に書き込まれるようにする。
*   **Brigadierコマンドの実装:** `gamerule`コマンドをBrigadierを使って実装する。`literal("gamerule").then(...)`)の形で、引数なしで全ルールを表示する、`gamerule ruleName`で特定のルールの値を表示する、`gamerule ruleName value`でルールの値を変更する、という3つのサブコマンドを定義する。値の変更コマンドは、`GameRuleManager`の`setGameRule`関数を呼び出す。

第四に、**他にも永続化が必要なデータの追加**である。`cpp-fabricmc`は`poi/*.mca`（興味のある場所）、`advancements`（実績）、`statistics`（統計情報）といったファイルも永続化していない 。

*   **`PoiManager`の実装:** 村人や職人の職業ブロック（例：工作台、畑）の位置を`poi`ファイルに保存するロジックを追加する。これにより、村人が昼間になると職業ブロックに戻るといった振る舞いが実現できる。
*   **`AdvancementManager`の実装:** 実績の進捗状況をJSONで定義された`advancements`データを読み書きするためのロジックを追加する。
*   **`StatisticsManager`の実装:** 統計情報の更新を監視し、一定時間ごとに`level.dat`に保存するロジックを追加する。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **region format** | 同一フォーマット  | 相互運用可能 (検証済)  | 保持 |
| **level.dat** | 書き出しなし  | ゲームルール/スポーン/統計を保存 [[136]] | `LevelData`コンテナ、`LevelDataManager`リポジトリ |
| **playerdata** | なし  | プレイヤーNBT(position/inventory/health)を保存 [[136]] | `PlayerData`コンテナ、`PlayerDataManager`リポジトリ |
| **entities/*.mca** | なし  | Mob永続化 [[90]] | `EntitySerializer`、`EntityPersistenceService` |
| **game rule コマンド** | なし  | `/gamerule`で全ルール変更 [[43]] | `GameRule`/`GameRuleManager`、Brigadierコマンド実装 |
| **statistics** | なし  | 統計データを保存 [[136]] | `StatisticsManager`の実装 |

これらの永続化機構を網羅的に実装することで、`cpp-fabricmc`は一時的な遊び場ではなく、長期的な進行が可能な本格的なゲーム世界となる。サーバーの再起動がプレイヤーの努力を帳消しにするという致命的な欠陥は解消され、安定した運営が可能になる。

## アーキテクチャの最適化と将来展望：パフォーマンス向上と次世代サーバーへの道筋

`cpp-fabricmc`が前述の各機能領域で本家と同等の振る舞いを再現するための技術的改善を進める中で、アーキテクチャ全体の最適化と将来の成長性を鑑みた設計が不可欠となる。現在の実装は、Nettyイベントループを採用したJava版とは対照的に、接続ごとに独立したスレッドを生成する`thread-per-connection`モデルを採用している可能性があり、大規模な同時接続に対するスケーラビリティでは劣位に立つ恐れがある 。また、C++ならではの手動メモリ管理は高速性をもたらす一方で、メモリリークのリスクも伴う [[237]]。本章では、ネットワーク層の完成、パフォーマンスと安定性の向上策、そして`cpp-fabricmc`の将来展望について考察し、より高性能で信頼性の高いサーバーへと進化させるための総括的な提言を行う。

まず、**ネットワーク層の完成**である。`cpp-fabricmc`は、オンライン認証の暗号化、パケット圧縮、クライアントからのリソースパック要求など、現代のMinecraftサーバーとして必要な多くのネットワーク機能を未対応としている <user_conversation>。これらを実装することで、セキュリティ、パフォーマンス、そして拡張性が大幅に向上する。

*   **圧縮とChat Signing:** パケットサイズを削減し、通信効率を高めるため、zlibライブラリをリンクし、256バイトを超えるパケットに対して自動的に圧縮・展開するロジックを追加する [[158]]。また、1.19以降のクライアントとの互換性を保つため、チャットメッセージにSHA-256署名を付与する`Chat signing`のロジックを実装する [[158]]。
*   **Resource PackとCookieの対応:** クライアントにテクスチャやモデルの差し替えを可能にするため、リソースパックをサーバーから送信し、応答を受信するためのネットワークハンドラーを追加する [[151]]。さらに、ログインフェーズでクライアントから送信される`Cookie`を処理するロジックを実装することで、特定のModやプラグインとの連携が可能になる [[158]]。

次に、**パフォーマンスと安定性の向上**である。C++の強みを最大限に活かすためには、厳格なメモリ管理と効率的なデータ構造の利用が鍵となる。

*   **RAIIとスマートポインタの徹底:** JavaのGarbage Collectorによるメモリ管理とは異なり、C++では手動でのメモリ解放が求められる。これによりメモリリークが発生しやすい。`new`/`delete`の使用を最小限に抑え、代わりに`std::unique_ptr`や`std::shared_ptr`といったスマートポインタを積極的に用いることで、オブジェクトのライフサイクルを自動的に管理し、リークを防ぐ [[237]]。また、`std::vector`や`std::string`といったコンテナの再確保によるメモリコピーを避けるために、`reserve()`の事前呼び出しや、移動semantics(`std::move`)の活用も重要である [[240]]。
*   **Chunk GC（ガベージコレクション）の実装:** 大規模なワールドを扱うサーバーでは、メモリ上に常駐させるべきチャンクの数に上限を設け、利用されていない古いチャンクを解放する仕組みが必要となる。`thread-per-connection`モデルでは特に重要である。最近使用履歴(LRU)キャッシュや非利用キューを用いて、プレイヤーから離れたチャンクを非同期でアンロードし、メモリ消費量を抑えるロジックを実装する [[40]]。
*   **プロファイリングツールの導入:** 性能を最適化するためには、問題の箇所を正確に特定することが不可欠である。ValgrindやheaptrackといったC++向けのメモリプロファイラを導入し、メモリリークや低速なメモリアクセスを検出する [[92,98]]。また、perf（Linux）やVTune（Windows）といったCPUプロファイラを用いて、CPU時間を多く消費している関数を特定し、チューニングの優先順位を決める [[97,354]]。これにより、無駄な最適化を避け、実際に性能向上に繋がるコード修正を行うことができる。

最後に、**将来展望**である。`cpp-fabricmc`の目標は、単に本家を再現することではなく、C++という言語の特性を活かした、次世代のMinecraftサーバーの原型を作ることにある。JavaのJVMが抱えるメモリ消費量の問題 [[149]] を根本的に解決する可能性を秘めている。

*   **ネイティブバイナリへのコンパイル:** GraalVMのNative Imageのような技術を活用し、`cpp-fabricmc`をネイティブな実行可能ファイル（exeやELF）にコンパイルする。これにより、JVMの起動オーバーヘッドが無くなり、アプリケーションの起動が劇的に高速化されるだけでなく、実行時のメモリ使用量も大幅に削減される可能性がある [[102,104]]。これは、低スペックな環境やクラウド上でのコスト削減において大きな利点となる。
*   **並列処理の深化:** C++の並列処理ライブラリ（例：C++17の`std::thread`, Intel TBB）を活用し、ワールド生成、チック処理、ネットワークI/Oをより細かくスレッドに分散させる。例えば、`Chunk GC`のように、メインサーバースレッドの負荷を軽減する非同期処理を増やしていくことで、より多くのプレイヤーを捌けるスケーラブルなアーキテクチャへと進化できる [[255]]。
*   **データ指向アーキテクチャの導入:** MobのAIロジックやエンティティの更新処理など、CPUキャッシュの局部性を悪化させる散発的なメモリアクセスを改善するため、データ指向アーキテクチャの原則を適用する。例えば、同じ種類のエンティティ（例：全てのゾンビ）のデータをメモリ上に連続して配置する（AoS: Array of Structures -> SoA: Structure of Arraysへの変換）ことで、CPUのプリフェッチ機構が効率的に働き、性能を向上させることができる [[186,259]]。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **スレッドモデル** | thread-per-connection  | Nettyイベントループ + サーバースレッドモデル [[10]] | スレッドプールと非同期タスクキューの導入 |
| **Chunk GC** | なし  | LRU + unload
# C++によるMinecraftサーバー実装cpp-fabricmcの本家server.jar代替化戦略：11分野の網羅的分析とアーキテクチャ設計

本レポートは、オープンソースのC++によるMinecraftサーバー実装である`cpp-fabricmc` [[1,2]] が、公式のJava版`server.jar`と比較して存在する機能ギャップを網羅的に分析し、その解消に向けた体系的かつ詳細な技術的改善計画を提示することを目的とする。ユーザーの要求として、「どれだけ長くなっても良い」「時間がかかっても良い」という無制限の時間的余裕が許容されていることから、単なるバグ修正や軽微な機能追加に留まらず、`cpp-fabricmc`を完全な代替品として位置づけるための、モジュール構成、データフロー、状態管理、イベントハンドリングを含む包括的なアーキテクチャ設計を提供することが必須である <user_request>。特に、インベントリ操作の反映、ブロック更新伝播、MobのAIと永続化、Brigadierベースのコマンドシステムといった高影響度項目に深く焦点を当てる <user_request>。本レポートは、現在の開発チームだけでなく、将来的なコントリビューターも想定し、技術的背景の有無にかかわらず理解可能な完全性を目指す。

## ワールド生成と地形の再現：データ駆動型設計による形態学からの脱却

`cpp-fabricmc`の現状において、最も顕著なギャップはワールド生成領域にある <user_conversation>。地形形状、バイオーム配置、構造物、鉱石分布など、全ての要素が本家と大きく異なる。この差異の根本原因は、地形生成の背後にあるアルゴリズムの違いにある。本家では、地形の密度、表面の形状、バイオームの選択、構造物の配置といった一連のプロセスが、全てJSONファイルで定義された複雑なノイズ関数と条件式によって駆動されている [[148,203]]。これに対し、`cpp-fabricmc`は独自のPerlinオクターブ近似のような簡易的なノイズ関数を使用している可能性が高い <user_conversation>。このため、同一のシードでも地形が全く異なる結果となり、探索要素が失われている <user_conversation>。本章では、これらのギャップを解消するための、データ駆動型のアーキテクチャ設計を提案する。

まず、地形生成の核となる「密度関数パイプライン」の再現から始める。本家の地形生成は、`overworld.json`といったJSONファイルで定義された巨大な木構造を持つ密度関数グラフによって制御されている [[203]]。このグラフは、複数のノイズ源（例：base_3d_noise, factor, jaggedness）を加算、減算、スケーリング、クリッピングといった数学的な演算を通じて組み合わせることで、最終的な地形の密度値を計算する [[203]]。例えば、「sloped_cheese」密度関数は、大陸性、侵食、山脈と谷のノイズを組み合わせて表面の高さと形状を決定し、`depth`密度関数はY座標の勾配とノイズを足して一般の地表高を決定する [[203]]。`cpp-fabricmc`がこれを実装するには、以下の3つの技術的ステップが必要となる。

第一に、**データ駆動型のアーキテクチャ設計**である。`density_function`ディレクトリ以下に配置されるJSONファイルを読み込み、それをC++で実行可能なオブジェクトに変換するためのVM（バーチャルマシン）またはインタプリタを構築する必要がある。これは、Java版の`net.minecraft.world.level.levelgen.DensityFunction`クラス群の役割を模倣するものである。このアーキテクチャでは、`DensityFunctionRegistry`という静的クラスが登録済みの関数タイプ（例：`minecraft:add`, `minecraft:noise`）を識別し、`DensityFunctionParser`がJSONの構文を解析して対応する`DensityFunction`派生クラスのインスタンスを生成する。各`DensityFunction`クラスは、`evaluate(StructureWorldAccess world, double x, double y, double z)`といった共通インターフェースを持ち、再帰的に子ノードの評価を呼び出すことで、最終的な密度値を計算する。この設計により、新しい密度関数の追加や既存の関数の修正が、コードの再コンパイルなしにJSONファイルの編集だけで可能になる。

第二に、**性能を考慮した実装手法の選択**である。純粋にJSONを毎回パースするのではなく、一度読み込んだJSONをパースし、内部で効率的なデータ構造（例えば、ポインタで結ばれた計算グラフ）に変換するプロセスを導入する。さらに、開発初期段階での開発効率を高めるために、PythonやLuaなどのスクリプト言語を使ってJSONを解析し、C++用のシリアライズ済みバイナリデータ（例：`.dfc`ファイル）を出力するツールチェーンを開発することも有効である。このバイナリデータは、ランタイムでのパースコストを大幅に削減できる。ただし、最終的にはJSON形式を直接サポートするロジックを実装し、データパックによるカスタム生成の柔軟性を維持する必要がある。

第三に、**依存する他のノイズ関数との統合**である。密度関数は単独で動作するのではなく、大陸性、湿度、侵食、浸食、深さ、奇妙さといった6つの主要なパラメータを元に動作する [[148]]。これらはそれぞれ別のノイズマップから生成され、密度関数内で`minecraft:noise`関数によってアクセスされる。そのため、`cpp-fabricmc`は、これらの基本的な6つのパラメータを計算するためのノイズ生成器（例：Perlinノイズ、Simplexノイズの実装）も併せて実装する必要がある。これにより、密度関数が期待通りの値を受け取り、地形の広域的な特徴（例：大陸、海岸線、山脈）を正しく表現できるようになる。

次に、**バイオーム配置**の再現である。本家では、最終的な地形の密度値や温度、湿度、大陸性といったパラメータを元に、`MultiNoiseBiomeSource`がどのバイオームがその座標に割り当てられるかを決定する [[148]]。`cpp-fabricmc`は、上記で実装した密度関数の評価結果を`BiomeSource`クラスに渡し、`getBiomes()`メソッド内でMojoの`MultiNoiseBiomeSource`の振る舞いを模倣するロジックを実装する。このロジックは、定義済みのバイオーム候補の中から、各座標の6軸のパラメータ値が収まる範囲のものを選択する。この際、`BiomeModifications` APIのような、データパックでバイオームの特性を変更したり、新しい特徴を追加したりするためのAPIを提供することが望ましい [[83]]。

構造物の生成も同様に、データ駆動型で実装すべき重要な要素である。本家では、村、廃墟、遺跡、ダンジョンといった多数の構造物が、`configured_structure_feature`と`placed_feature`という二段階のJSON定義で管理されており、生成ステップ（例：`STRUCTURE_START`, `FEATURE`）に従って生成処理が行われる [[148]]。`cpp-fabricmc`は、この仕組みを再現するために、`Feature`と`PlacedFeature`という抽象基底クラスを定義する。`Feature`は、`generate()`という仮想関数を持ち、その派生クラス（`StructureFeature`, `TreeFeature`など）で具体的な生成ロジックを実装する。一方`PlacedFeature`は、特定の`Feature`のインスタンスと、その生成条件（例：出現回数、高さ制限、特定のブロック上のみ生成）を保持する。`GenerationStep`列挙型に対応するハンドラーを用意し、各ステップで対応する`PlacedFeature`のリストを走査して生成処理を呼び出す。構造物自体は、`structure_piece`のJSON定義をパースし、`StructureComponent`の派生クラスとして実装された部品の集合として扱うことで、複雑な建物も正確に再現できる [[148]]。

最後に、**鉱石分布**の再現である。本家では、`ore_vein_type`という定義されたパターンに従って鉱石が生成され、それが`CountPlacement`や`HeightRangePlacement`といった配置modifierによって出現頻度や高さ帯が調整される [[148]]。`cpp-fabricmc`は、この`PlacementModifierType`の概念をC++で再現する。`PlacementModifier`というインターフェースを定義し、`CountPlacementModifier`, `InSquarePlacementModifier`などの具体的な実装を作成する。これにより、`PlacedFeature`の生成ロジック内で、順番にこれらのmodifierを適用することで、煤や鉄鉱石が洞窟内や地上でどのように分布するかといった複雑な挙動を表現できる。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **密度関数パイプライン** | 独自Perlinオクターブ近似 [[49]] | JSON定義の密度関数グラフをパースし実行するVM/インタプリタ [[203]] | `DensityFunctionRegistry`, `DensityFunctionParser`, 各`DensityFunction`派生クラスの実装 |
| **バイオーム配置** | 単一biome(plains固定) [[39]] | MultiNoiseBiomeSourceの振る舞いを模倣し、JSON定義に基づいて配置 [[148]] | `BiomeSource`クラスの実装、`BiomeModifications` API準拠の拡張機構 |
| **構造物生成** | なし | 各種構造物をステップベースでJSON定義に従って生成 [[148]] | `Feature`/`PlacedFeature`の設計、`StructureComponent`の実装、ステップごとのハンドラー登録 |
| **鉱石分布** | 単純ノイズ閾値 [[49]] | `ore_vein_type` + `PlacementModifierType`で構成される多様な分布を表現 [[148]] | `PlacementModifier`インターフェースとその実装の導入 |

これらの改善を段階的に実装することで、`cpp-fabricmc`は単なる幾何学的な地形から、生物群落の多様性、探索の楽しみ、資源採取の戦略性を備えた本格的なMinecraftの世界へと進化していく。これは、あらゆる上位レイヤーのゲームプレイが成立するための不可欠な土台となる。

## 物理法則と世界のダイナミクス：イベント駆動型アーキテクチャによる再構築

`cpp-fabricmc`の現状では、世界の物理法則が極めて単純化されており、大きな機能ギャップとなっている <user_conversation>。ブロックの設置以外に変化がなく、水が流れない、砂が落下しない、レッドストーンが動かないといった問題は、単なる未実装機能の問題ではなく、システムの根本的な設計思想の違いに起因している。本家Minecraftは、あるブロックの状態が変化したときに、その変化が隣接するブロックに伝播するという「更新伝播」の仕組みを基盤としている [[194]]。本章では、このイベント駆動型のアーキテクチャを採用し、水の流れ、砂の落下、明るさの伝播といったダイナミックな世界の物理法則を`cpp-fabricmc`に再構築するための詳細な技術的改善方針を提示する。

第一に、**ブロック更新伝播システムの設計**である。これは、全てのダイナミックなブロック挙動を実現するための基盤となる。本家の仕組みでは、あるブロックが更新されると（例：水が湧き出る、砂が倒れる）、`AbstractBlock.neighborUpdate`メソッドが呼び出され、そのブロック自身が隣接するブロックに対して、自分自身の更新が起こったことを通知する [[281]]。この通知を受け取った隣接ブロックは、自身の状態を更新する必要があるかどうかを判定し、必要であればさらにその隣接ブロックに通知を伝播させる [[194]]。`cpp-fabricmc`がこれを実装するには、以下のようなアーキテクチャを構築する必要がある。

*   **`BlockState`と`BlockBehavior`の分離:** `Block`クラスを抽象化し、ブロックの静的な特性（名前、硬度など）と、動的な振る舞い（落下、浸透など）を分離する。`BlockBehavior`という抽象基底クラスを定義し、`FallingBlockBehavior`, `WaterloggableBehavior`といった具象クラスを用意する。これにより、各ブロックが持つ振る舞いを独立して管理・拡張できる。
*   **`BlockUpdateEvent`の定義:** 更新が発生した際の情報を保持するためのイベントクラス`BlockUpdateEvent`を定義する。このイベントには、更新が発生した座標、更新元となったブロック情報などが含まれる。
*   **更新キューの導入:** 各チックごとに処理される`World::tick()`関数内で、更新キューというキュー型のデータ構造を用意する。更新が発生した際には、その座標とブロック情報をこのキューに積む。その後、チック終了までにキューに積まれたすべての更新について、順次処理を行う。これにより、同時に多数の更新が発生しても安定して処理を進めることができる。更新の処理は、まず`BlockUpdateEvent`をイベントシステムに発行し、それに反応した各種の`BlockBehavior`が更新を処理するという形にする。

第二に、**水の流れの再現**である。本家では、流れる水は単一のブロック状態（`flowing_water`）として存在するのではなく、個々のエンティティとして扱われている [[76]]。このエンティティは、毎チック重力を受けて下方に移動し、支持体がない場合に限り水平方向にも動き、一定条件下で静止水（`water`）に変化する [[77,79]]。`cpp-fabricmc`は、この概念を`FallingBlockEntity`という独立したエンティティクラスとして実装する。このエンティティは、`update()`関数内で重力を適用し、周囲のブロック状態をチェックして落下または水平移動を判定する。また、`onLand()`などのコールバック関数を用意し、地面に着地した際に、自身の座標のブロックを静止水に変更するロジックを含める。これにより、水が自然に低い方へ流れていき、湖を形成するダイナミックな挙動が再現される。さらに、`WaterBlock`が更新された際にこのエンティティを生成するロジックを追加すれば、水の湧出といったより複雑な挙動も可能になる [[196]]。

第三に、**砂・砂利の落下**である。これは上記の`FallingBlockEntity`の概念を流用することで容易に実装できる。砂や砂利は、`GravityAffectedBlock`のようなフラグを持つブロックとして定義され、その下のブロックがなくなる（空気や液体）と、`FallingBlockEntity`が生成される。このエンティティは、`update()`関数内で重力を継続的に適用し、落下速度を増していく。地面に着地した瞬間、エンティティは削除され、その座標に着地した結果として生まれるブロック（例：砂は砂岩に、砂利は普通の岩に）を設置する [[176,177]]。この実装により、サンドキャンドルやサンドウォールの崩壊、サンドデューパーといったレッドストーン回路の一部として利用される落下現象が正確に再現される。

第四に、**レッドストーンの完全シミュレーション**である。レッドストーンは、信号の伝播、強度の減衰、パルスの生成といった複雑な論理回路を構成するためのシステムであり、それは全てブロック更新の伝播によって駆動されている [[209,248]]。例えば、赤石粉が更新されると、その隣の赤石粉や赤石タルタルに更新が伝わり、信号が伝播する。赤石TORCHが更新されると、その出力がON/OFFし、接続された機械を制御する。`cpp-fabricmc`は、上記で設計した更新伝播システムを基盤とすることで、レッドストーンの全ての振る舞いを再現できる。具体的には、赤石粉、赤石TORCH、赤石レバー、ピンボールなど、全ての赤石関連ブロックに対して`neighborUpdate`のロジックを実装し、信号の伝播と強度の計算を行う。例えば、`RedstoneWireBlock`の`getStateForNeighborUpdate`は、接続先の赤石粉に信号を伝播させる更新を発行する。また、`Observer`のような、特定の更新を検知してパルスを発生させる特殊なブロックも実装する必要がある [[297,298]]。これにより、単純な動かないレッドストーンから、複雑な時計回路やメモリ回路まで、ほぼ全てのレッドストーン装置が動くようになる。

第五に、**明るさシステムの再現**である。本家では、光源（例：松明、ダイヤモンド）が設置されると、そのブロック自体が`block light`を放出し、それが周囲に伝播していく。また、空からの光である`sky light`も、雲の有無や垂直方向の距離に応じて計算される。この両方が合成され、各ブロックの明るさが決定される [[256]]。`cpp-fabricmc`は、このシステムを`LightEngine`という独立したモジュールとして実装する。このエンジンは、`SkyLightStorage`と`BlockLightStorage`という2つのChunkDataレイヤーを持つ。光源ブロックが設置または削除されると、`LightEngine`はその周囲のライト値を更新する「エッジ」をキューに積み、BFS（幅優先探索）で伝播させていく。これにより、松明の周りに影がつき、洞窟の奥行きが表現されるリアルな照明が実現される。`level.dat`に保存される自動保存設定は、本家よりも高い頻度（3秒毎）で実装されているが、これは望ましい改善点である 。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **ブロック更新伝播** | なし(単一ブロックのみ変更) [[194]] | 更新が隣接ブロックにイベント駆動で伝播 [[193]] | `BlockUpdateEvent`、更新キュー、`neighborUpdate`コールバックの実装 |
| **水の流れ** | なし(静的) [[76]] | `FallingBlockEntity`を用いたエンティティとしての水流 [[77]] | `FallingBlockEntity`クラスの実装、重力と衝突判定の追加 |
| **砂・砂利落下** | なし [[176]] | `FallingBlockEntity`を用いた落下と着地後のブロック変化 [[177]] | 上記`FallingBlockEntity`の流用、`onLand`コールバックの実装 |
| **レッドストーン** | なし [[209]] | 更新伝播を基盤とした信号の完全なシミュレーション [[248]] | 各レッドストーンブロックでの`neighborUpdate`ロジックの実装 |
| **明るさシステム** | sky light近似、block light未実装 [[256]] | Sky LightとBlock Lightの両方を伝播計算 [[256]] | `LightEngine`モジュール、`SkyLightStorage`/`BlockLightStorage`の実装、BFS伝播ロジック |

これらの物理法則の再構築は、`cpp-fabricmc`を単なる建築プラットフォームから、プレイヤーの創造性が試されるダイナミックなsandboxへと昇華させる上で不可欠なステップである。

## エンティティとMobの復活：永続化とAIを支えるエンティティシステムの設計

`cpp-fabricmc`の現状では、Mobはゾンビや動物といった5種類に限定され、高度なAIや永続化といった本家に見られる重要な要素が欠落している <user_conversation>。Mobは単一の行動しかせず、仲間やプレイヤーとの相互作用もなし、サーバーを再起動すれば全て消えてしまう。これは、エンティティシステムが非常に単純化されているためである。本章では、MobのAI、行動セット、永続化、そしてMob間の相互作用を実現するための包括的なエンティティシステムのアーキテクチャ設計を詳細に提示する。

第一に、**MobのAIと行動セット（ゴールシステム）の実装**である。本家MinecraftのMobは、単一の行動ではなく、攻撃、逃走、歩き回る、餌を与えるといった複数の「目標」を持ち、それらを優先度に基づいて切り替えながら行動する [[19]]。この「脳」の概念を`cpp-fabricmc`に実装するには、以下の3つの主要なクラスを設計する。

*   **`Goal`（目標）抽象基底クラス:** `Goal`は、`canStart()`, `shouldContinue()`, `start()`, `tick()`, `stop()`といった純粋仮想関数を持つ抽象クラスである。これにより、各行動（例：`MeleeAttackGoal`, `WanderGoal`, `MateGoal`）が独立したクラスとして実装でき、交換可能になる。
*   **`Brain`（脳）クラス:** `Brain`は、現在実行中の`Goal`を管理する役割を持つ。`update()`関数内で、登録されている全ての`Goal`の`canStart()`を評価し、成功した最初の`Goal`を選択して実行を開始する。もし現在実行中の`Goal`の`shouldContinue()`が偽を返せば、`stop()`を呼び出し、次のチックで再度`canStart()`の評価を再開する。これにより、行動の優先順位付けと状態遷移が実現される。
*   **`Pathfinder`（経路探索）:** Mobが目的地まで移動するためには、A*アルゴリズムを用いた経路探索が必要である。`Pathfinder`クラスを実装し、`findPath(Entity from, BlockPos to)`といった関数を提供する。このクラスは、ブロックの通過ペナルティ（例：水中ではペナルティが高い）を考慮し、Mobが自然な動きで目的地まで到達するための経路を計算する [[27]]。

第二に、**エンティティの永続化**である。現在の`cpp-fabricmc`では、Mobのデータはサーバー再起動時に全て破棄される 。これを解決するためには、全てのエンティティ、特にMobとプレイヤーのデータをRegionファイルに定期的に書き込む仕組みが必要である。このためのアーキテクチャは以下の通りである。

*   **エンティティシリアル化:** 各`Entity`の派生クラス（`PlayerEntity`, `ZombieEntity`, `CowEntity`など）に、`serialize(NbtCompound& nbt)`と`deserialize(const NbtCompound& nbt)`という2つの関数を実装する。この関数群は、エンティティ固有のデータ（例：HP、飢餓度、持ち物、Mobのアイデンティティなど）をNBT（ネイティブバイナリタグ）形式のデータ構造にエンコーディングし、デコーディングする役割を持つ。
*   **永続化サービスの実装:** `EntityPersistenceService`のような静的サービスクラスを作成する。このサービスは、`Server::saveAll()`関数で呼び出される。この関数は、存在する全てのエンティティを走査し、`PlayerEntity`の場合は`playerdata`ディレクトリに、`MobEntity`の場合は`entities`ディレクトリに、エンティティIDをファイル名とした`.dat`ファイルにシリアル化したデータを書き込むロジックを持つ。これにより、サーバー停止時や自動保存タイマーが期限切れになった際に、エンティティの状態が安全に保存される [[136]]。

第三に、**Mob間の戦闘と相互作用**の実装である。動物がゾンビから逃げたり、村人たちが職業ブロックで働いたりするためには、Mob間の関係性と、それがAIに与える影響を定義する必要がある。

*   **敵対関係の定義:** `EntityType`に`SpawnGroup`（例：CREATURE, MONSTER, AMBIENT, WATER_CREATURE）という列挙型を関連付ける。そして、`EntityType`同士の敵対関係を判定する静的な関数`isHostileTo(EntityType a, EntityType b)`を実装する。例えば、`SpawnGroup::MONSTER`のMobは`SpawnGroup::CREATURE`のMobに対して常に敵対的であるとする。
*   **AIとの連携:** `Brain`内の`checkForNearestEnemy()`という`Goal`を実装する。この`Goal`は、`isHostileTo()`関数を呼び出して、周囲にいるMobが自分にとって敵対的かどうかを確認する。もしプレイヤーや仲間の敵対Mobを見つけたら、攻撃や回避といった行動を開始する。これにより、ゾンビがプレイヤーを襲う、狼がゾンビを攻撃するといった自然な生態系が再現される。

第四に、**Mobの多様性と種族の拡張**である。現在の5種類から74種類以上に増やすには、Mobの定義をデータ駆動で行う仕組みが必要である。

*   **データ駆動のMob定義:** Mojangの`entity_type`のJSON定義を参考に、`cpp-fabricmc`でも同様のJSONファイルでMobの定義を行う。このJSONには、Mobの表示名、エンティティクラス名、最大HP、被ダメージ量、スポーン条件、レシピ（繁殖素材）などが記述される。
*   **MobRegistryの導入:** `MobRegistry`という静的クラスを用意し、起動時に`entity_type`ディレクトリ配下のJSONファイルを読み込んで、エンティティタイプのIDとその定義をマッピングする。これにより、Modのように新たなMobを追加することができる。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **Mob AI** | 単純wander/zombie chase のみ [[19]] | Priority-based Goal System + Pathfinding [[19]] | `Goal`抽象クラス、`Brain`クラス、A*アルゴリズムの実装 |
| **Mob間戦闘** | なし | EntityTypeごとに敵対関係が定義され、AIが反応 [[44]] | `SpawnGroup`の定義、`isHostileTo()`関数、`Brain`との連携 |
| **エンティティNBT永続化** | **Mobはメモリ上のみ** [[90]] | Region fileにエンティティデータを保存 [[90]] | `serialize`/`deserialize`メソッド、`EntityPersistenceService`の実装 |
| **Mob種類** | 5種類(pig/cow/sheep/chicken/zombie) | 74種類以上、JSON定義による拡張 | `EntityType`のJSON定義、`MobRegistry`の実装 |
| **繁殖** | なし | 餌付け→子産み [[169]] | `MateGoal`の実装、JSONでの繁殖素材定義 |
| **装備** | なし | 手持ち/防具/ドロップ [[169]] | `MobEntity`に`EquipmentComponent`を追加、ドロップロジックの実装 |

これらの改善を実施することで、`cpp-fabricmc`の世界は生命に満ちた、予測不能で魅力的な環境へと変貌する。Mobは単なる障害物ではなく、生態系の一員としてプレイヤーの冒険を彩る存在となる。

## ゲームプレイの基盤：インベントリ、クラフト、およびコマンドシステムの一体化

`cpp-fabricmc`の現状では、高度なゲームプレイの基盤となるシステムがほとんど実装されていない <user_conversation>。インベントリ操作がサーバーに反映されず、アイテムを移動・クラフトできない、そしてコマンドは`/help`や`/ping`程度しか使えない。これは、サーバーとしての機能が極端に不十分であることを意味する。本章では、これら三つの核心的なシステム——インベントリ操作の同期、クラフトロジックの実装、そしてBrigadierベースのコマンドシステムの再現——を一体化したアーキテクチャで設計し、`cpp-fabricmc`を本格的なマルチプレイヤーゲーム環境へと昇華させるための技術的改善方針を詳述する。

第一に、**インベントリ操作の反映とUIの同期**である。現在、`cpp-fabricmc`はクライアントが送信する`WindowClickC2S`パケットを受信した後、何も処理せずに破棄している <user_conversation>。これでは、プレイヤーがドラッグ＆ドロップやシフト＋クリックでアイテムを移動させても、サーバー上のインベントリとクライアント上のUIが完全にデシンクロナイズされ、ゲームプレイが不可能になる [[13,17]]。この問題を解決するためには、クライアントからの操作を正確に解釈し、サーバーの状態を更新した上で、その結果をクライアントに通知する双方向の通信フローが必要である。

*   **Packet Handlerの実装:** `WindowClickC2S`パケットを受信するハンドラー関数を実装する。この関数は、パケット内のウィンドウID、スロット番号、マウスボタン、操作ID、アイテムスタックなどを正確に解析する。
*   **InventoryManagerの導入:** 各エンティティ（プレイヤー、チェスト、かまどなど）が持つインベントリを表す`InventoryComponent`というコンポーネントを定義する。このコンポーネントは、アイテムスタックの配列（例：`std::array<ItemStack, 36>`）を内部に保持し、アイテムの追加、削除、置き換えといった基本操作を提供する。
*   **操作のロジック分離:** 解析したパケットの内容に応じて、`InventoryManager::handleSlotClick()`や`InventoryManager::handleDragDrop()`といった関数を呼び出し、対応するロジックを実行する。例えば、シフトクリックは自動的にインベントリの空いているスロットにアイテムを移動させるロジックになり、ドラッグ＆ドロップはウィンドウ間でのアイテム移動を表す。
*   **UIの同期:** インベントリの操作がサーバー上で完了すると、直ちにその結果をクライアントに通知する。`ContainerSetContentS2C`パケットでウィンドウ内の全アイテムを再送信し、`ContainerSetSlotS2C`パケットで変更された特定のスロットのみを更新する。これにより、クライアントのUIがサーバー上の状態とリアルタイムで同期される [[174]]。

第二に、**クラフトとコンテナUIの実装**である。工作台や醸造台といった拡張UIを開き、そこに置かれたアイテムを使ってクラフトする機能を追加する。

*   **コンテナUIの実装:** チェスト、工作台、かまど、醸造台など、複数のスロットを持つコンテナは、全て同じ`Container`クラス（またはインターフェース）を継承するように設計する。`Container`は、スロット数やUIのタイトルを定義する。プレイヤーが右クリックした際、該当する`BlockEntity`（チェストなど）がサーバーに存在するかを確認し、存在すれば対応する`Container`インスタンスを作成し、クライアントに`OpenContainerS2C`パケットを送信する。これにより、クライアントは対応するGUI（ユーザーインターフェース）を開くことができる [[128]]。
*   **Recipe RegistryとCraftingManager:** `recipes`ディレクトリ配下のJSONファイルを読み込み、`Recipe`という抽象基底クラスを実装する。`ShapedRecipe`, `ShapelessRecipe`といった具体的なレシピクラスを用意する。これらを登録する`RecipeRegistry`と、`craftItem(Inventory input, Recipe recipe)`といった関数でクラフト結果を計算する`CraftingManager`を実装する。これにより、JSONで定義されたあらゆるクラフトパターンがサーバー上で認識され、実行可能になる [[116]]。
*   **UIとの連携:** クライアント側でクラフトボタンが押された際、クライアントは使用されたレシピの情報をサーバーに送信する。サーバー側の`CraftingManager`がそのレシピを検索し、入力インベントリから必要な素材を消費して、結果のアイテムを出力インベントリに追加する。この結果をクライアントに通知し、UIを更新する。

第三に、**Brigadierベースのコマンドシステムの再現**である。本家Minecraftのコマンドシステムは、Mojangが開発したBrigadierライブラリによって支えられており、複雑な構文、引数の検証、タブ補完、そして@aや@pといったターゲットセレクターを提供する [[29]]。`cpp-fabricmc`は、このBrigadierの振る舞いをC++で再現することで、本格的なサーバー管理者ツールを提供できる。

*   **ArgumentBuilderの移植:** Brigadierの構文木を構築するための`CommandNode`とその派生クラス`LiteralArgumentNode`, `RequiredArgumentNode`をC++で実装する。これにより、`dispatcher.register(literal("give").then(argument("targets", players()).build()))`といった構文で、コマンドツリーをプログラムで構築できる [[133]]。
*   **CommandDispatcherとExecutor Pattern:** `CommandDispatcher`クラスを実装し、登録されたコマンドツリーを走査するロジックを内包する。入力された文字列をBrigadierのアルゴリズムに従って解析し、対応する`CommandNode`にマッチするかを判定する。マッチした場合、そのノードが関連付けられた`CommandExecutor`（コマンド実行者のこと）の`execute()`メソッドが呼び出される [[33]]。
*   **標準引数型の実装:** `StringArgumentType`, `IntegerArgumentType`, `FloatArgumentType`, `PlayerArgumentType`といった標準の引数型を実装し、タブ補完のための`Suggestions`機能も提供する。これにより、ユーザーがコマンドを入力する際に、有効な引数候補を自動的に表示できる。
*   **Target Selectorのサポート:** `@a`, `@p`, `@e`, `@r`といったターゲットセレクターを解析するロジックを追加する。入力されたセレクターを`EntitySelector`クラスに渡すと、その条件に合うエンティティのリストを返す。これにより、特定のプレイヤーだけをターゲットにするなど、高度なコマンドが可能になる [[138,142]]。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **インベントリ操作** | 受信するが解析せず破棄 <user_conversation> | WindowClickPacketを完全に処理し、UI状態を同期 [[17]] | `WindowClickC2S`ハンドラー、`InventoryManager`、`Container`の実装 |
| **クラフト** | なし <user_conversation> | 3x3/2x2/工作台でCraftingTableRecipeが使用 [[116]] | `Recipe`/`CraftingManager`の実装、JSONレシピのロード |
| **拡張UI** | 開かない <user_conversation> | チェスト/かまど/作業台等のコンテナUIが開ける | `BlockEntity`（チェスト等）の実装、`Container`の実装、`OpenContainerS2C`の送信 |
| **Brigadier tree** | `/help` `/ping` のみ宣言 <user_conversation> | 完全なコマンドツリーを動的に構築 [[29]] | `CommandDispatcher`、`ArgumentBuilder`、`CommandExecutor`の実装 |
| **引数型** | 文字列分割のみ <user_conversation> | 全brigadier引数型（Integer, Playerなど）をサポート [[133]] | 各種`ArgumentType`の実装、`Suggestions`機能の追加 |
| **セレクター** | なし <user_conversation> | `@a`, `@p`, `@e`... を完全にサポート [[138]] | `EntitySelector`クラスの実装、セレクター文字列の解析ロジック |

これらのシステムを一体的に実装することは、`cpp-fabricmc`を単なる「遊びつくせるオープンワールド」から、「本家サーバーと同等の機能を備えた、創造的かつ戦略的なゲーム環境」へと引き上げる上で決定的な一歩となる。

## 永続化と環境設定：サーバー再起動後の記憶と世界のルール

`cpp-fabricmc`が本家`server.jar`の代替として機能するためには、サーバーの状態を再起動しても保持され、プレイヤーが自分の進捗を失わないような堅牢な永続化機構が不可欠である <user_conversation>。現在、`cpp-fabricmc`は`level.dat`や`playerdata`/*.datといった主要な永続化ファイルの書き出しを行っておらず、これによりゲームルールの変更や難易度、プレイヤーのスポーン地点、インベントリ、健康状態などが全てリセットされてしまう 。また、ゲームルールを動的に変更する`/gamerule`コマンドも実装されていない 。本章では、これらの永続化と環境設定の問題を解決するための、データコンテナ、リポジトリパターン、そしてゲームルールシステムの包括的なアーキテクチャ設計を提示する。

第一に、**サーバー全体の設定と統計情報を保持する`level.dat`の実装**である。本家では、このファイルがサーバーの基本的な設定を保持する。`cpp-fabricmc`は、この振る舞いを再現するための`LevelData`クラスを設計する。

*   **`LevelData`データコンテナの設計:** `LevelData`というC++クラスを定義し、メンバ変数として以下の情報を保持する。
    *   **ゲームルール (`GameRules`):** `doDaylightCycle`, `pvp`, `difficulty`など、全36のゲームルールの値を保持する [[42]]。これは、キーがゲームルール名、値がその設定値（真偽値、整数など）となるマップコンテナで実装される。
    *   **世界情報:** スポーンポイント、ワールドの作成日時、最終更新日時、統計情報の更新日時など。
    *   **統計情報 (`Statistics`):** プレイヤーが行った様々な行動のカウント（例：歩いた距離、アイテムを集めた回数など）を保持する [[136]]。
*   **`LevelDataManager`リポジトリの実装:** `LevelDataManager`という静的サービスクラスを作成する。このクラスは、サーバー起動時に`world/level.dat`ファイルを読み込み、`LevelData`オブジェクトをデシリアライズして保持する。また、`autosave`タイマー（本家は5分毎だが、cpp-fabricmcは3秒毎に設定されているが、これは望ましい改善点である ）が期限切れになったり、サーバーがシャットダウンする際に、`LevelData`オブジェクトをシリアル化し、`level.dat`に上書き保存するロジックを持つ。これにより、サーバー再起動後も全ての世界設定や統計情報が復元される。

第二に、**各プレイヤーの個別データを保持する`playerdata`ディレクトリの実装**である。プレイヤーの位置、健康状態、インベントリ、レベル、XPなど、プレイヤー固有の情報は`playerdata`ディレクトリ配下の個別の`.dat`ファイルに保存される [[136]]。

*   **`PlayerData`データコンテナの設計:** `PlayerData`というC++クラスを定義し、`UUID`をキーとしてインスタンスが管理される。このクラスには、プレイヤーの`Position`, `Inventory`, `Health`, `FoodTick`, `ExperiencеPoints`, `Level`など、プレイヤーの状態を表す全てのデータを保持する。
*   **`PlayerDataManager`リポジトリの実装:** `PlayerDataManager`という静的サービスクラスを作成する。このクラスは、`PlayerEntity`がログアウトした時や、一定時間経過時などに、対応する`PlayerData`オブジェクトを`playerdata`ディレクトリに保存するロジックを持つ。ログイン時には、そのプレイヤーのUUIDに対応する`.dat`ファイルを読み込み、`PlayerData`を復元し、`PlayerEntity`の初期状態として適用する。これにより、プレイヤーがサーバーを離れても、その進行状況が保持され、再ログイン時に以前の状態から続きが可能になる。

第三に、**動的に変更可能なゲームルールシステムの実装**である。本家では、`/gamerule`コマンドで`doFireTick`, `sendCommandFeedback`, `maxEntityCramming`といった多数のゲームルールをオン/オフまたは数値で変更できる [[43,44]]。`cpp-fabricmc`は、この振る舞いをBrigadierコマンドと永続化機構の連携で実現する。

*   **`GameRule`クラスの実装:** `GameRule<T>`というテンプレートクラスを定義し、`bool`, `int`, `float`といった異なる型のゲームルールを扱えるようにする。このクラスは、ルールのデフォルト値、現在の値、そして値のバリデーションロジックを保持する。
*   **`GameRuleManager`の実装:** `GameRuleManager`という静的サービスクラスを作成する。このクラスは、`LevelData`内の`GameRules`マップを操作する。`setGameRule(ruleName, value)`関数は、指定されたルール名の`GameRule`オブジェクトの値を変更し、それが有効な値かを検証する。変更が成功したら、`LevelData`の変更フラグを立て、次回の自動保存時に`level.dat`に書き込まれるようにする。
*   **Brigadierコマンドの実装:** `gamerule`コマンドをBrigadierを使って実装する。`literal("gamerule").then(...)`)の形で、引数なしで全ルールを表示する、`gamerule ruleName`で特定のルールの値を表示する、`gamerule ruleName value`でルールの値を変更する、という3つのサブコマンドを定義する。値の変更コマンドは、`GameRuleManager`の`setGameRule`関数を呼び出す。

第四に、**他にも永続化が必要なデータの追加**である。`cpp-fabricmc`は`poi/*.mca`（興味のある場所）、`advancements`（実績）、`statistics`（統計情報）といったファイルも永続化していない 。

*   **`PoiManager`の実装:** 村人や職人の職業ブロック（例：工作台、畑）の位置を`poi`ファイルに保存するロジックを追加する。これにより、村人が昼間になると職業ブロックに戻るといった振る舞いが実現できる。
*   **`AdvancementManager`の実装:** 実績の進捗状況をJSONで定義された`advancements`データを読み書きするためのロジックを追加する。
*   **`StatisticsManager`の実装:** 統計情報の更新を監視し、一定時間ごとに`level.dat`に保存するロジックを追加する。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **region format** | 同一フォーマット  | 相互運用可能 (検証済)  | 保持 |
| **level.dat** | 書き出しなし  | ゲームルール/スポーン/統計を保存 [[136]] | `LevelData`コンテナ、`LevelDataManager`リポジトリ |
| **playerdata** | なし  | プレイヤーNBT(position/inventory/health)を保存 [[136]] | `PlayerData`コンテナ、`PlayerDataManager`リポジトリ |
| **entities/*.mca** | なし  | Mob永続化 [[90]] | `EntitySerializer`、`EntityPersistenceService` |
| **game rule コマンド** | なし  | `/gamerule`で全ルール変更 [[43]] | `GameRule`/`GameRuleManager`、Brigadierコマンド実装 |
| **statistics** | なし  | 統計データを保存 [[136]] | `StatisticsManager`の実装 |

これらの永続化機構を網羅的に実装することで、`cpp-fabricmc`は一時的な遊び場ではなく、長期的な進行が可能な本格的なゲーム世界となる。サーバーの再起動がプレイヤーの努力を帳消しにするという致命的な欠陥は解消され、安定した運営が可能になる。

## アーキテクチャの最適化と将来展望：パフォーマンス向上と次世代サーバーへの道筋

`cpp-fabricmc`が前述の各機能領域で本家と同等の振る舞いを再現するための技術的改善を進める中で、アーキテクチャ全体の最適化と将来の成長性を鑑みた設計が不可欠となる。現在の実装は、Nettyイベントループを採用したJava版とは対照的に、接続ごとに独立したスレッドを生成する`thread-per-connection`モデルを採用している可能性があり、大規模な同時接続に対するスケーラビリティでは劣位に立つ恐れがある 。また、C++ならではの手動メモリ管理は高速性をもたらす一方で、メモリリークのリスクも伴う [[237]]。本章では、ネットワーク層の完成、パフォーマンスと安定性の向上策、そして`cpp-fabricmc`の将来展望について考察し、より高性能で信頼性の高いサーバーへと進化させるための総括的な提言を行う。

まず、**ネットワーク層の完成**である。`cpp-fabricmc`は、オンライン認証の暗号化、パケット圧縮、クライアントからのリソースパック要求など、現代のMinecraftサーバーとして必要な多くのネットワーク機能を未対応としている <user_conversation>。これらを実装することで、セキュリティ、パフォーマンス、そして拡張性が大幅に向上する。

*   **圧縮とChat Signing:** パケットサイズを削減し、通信効率を高めるため、zlibライブラリをリンクし、256バイトを超えるパケットに対して自動的に圧縮・展開するロジックを追加する [[158]]。また、1.19以降のクライアントとの互換性を保つため、チャットメッセージにSHA-256署名を付与する`Chat signing`のロジックを実装する [[158]]。
*   **Resource PackとCookieの対応:** クライアントにテクスチャやモデルの差し替えを可能にするため、リソースパックをサーバーから送信し、応答を受信するためのネットワークハンドラーを追加する [[151]]。さらに、ログインフェーズでクライアントから送信される`Cookie`を処理するロジックを実装することで、特定のModやプラグインとの連携が可能になる [[158]]。

次に、**パフォーマンスと安定性の向上**である。C++の強みを最大限に活かすためには、厳格なメモリ管理と効率的なデータ構造の利用が鍵となる。

*   **RAIIとスマートポインタの徹底:** JavaのGarbage Collectorによるメモリ管理とは異なり、C++では手動でのメモリ解放が求められる。これによりメモリリークが発生しやすい。`new`/`delete`の使用を最小限に抑え、代わりに`std::unique_ptr`や`std::shared_ptr`といったスマートポインタを積極的に用いることで、オブジェクトのライフサイクルを自動的に管理し、リークを防ぐ [[237]]。また、`std::vector`や`std::string`といったコンテナの再確保によるメモリコピーを避けるために、`reserve()`の事前呼び出しや、移動semantics(`std::move`)の活用も重要である [[240]]。
*   **Chunk GC（ガベージコレクション）の実装:** 大規模なワールドを扱うサーバーでは、メモリ上に常駐させるべきチャンクの数に上限を設け、利用されていない古いチャンクを解放する仕組みが必要となる。`thread-per-connection`モデルでは特に重要である。最近使用履歴(LRU)キャッシュや非利用キューを用いて、プレイヤーから離れたチャンクを非同期でアンロードし、メモリ消費量を抑えるロジックを実装する [[40]]。
*   **プロファイリングツールの導入:** 性能を最適化するためには、問題の箇所を正確に特定することが不可欠である。ValgrindやheaptrackといったC++向けのメモリプロファイラを導入し、メモリリークや低速なメモリアクセスを検出する [[92,98]]。また、perf（Linux）やVTune（Windows）といったCPUプロファイラを用いて、CPU時間を多く消費している関数を特定し、チューニングの優先順位を決める [[97,354]]。これにより、無駄な最適化を避け、実際に性能向上に繋がるコード修正を行うことができる。

最後に、**将来展望**である。`cpp-fabricmc`の目標は、単に本家を再現することではなく、C++という言語の特性を活かした、次世代のMinecraftサーバーの原型を作ることにある。JavaのJVMが抱えるメモリ消費量の問題 [[149]] を根本的に解決する可能性を秘めている。

*   **ネイティブバイナリへのコンパイル:** GraalVMのNative Imageのような技術を活用し、`cpp-fabricmc`をネイティブな実行可能ファイル（exeやELF）にコンパイルする。これにより、JVMの起動オーバーヘッドが無くなり、アプリケーションの起動が劇的に高速化されるだけでなく、実行時のメモリ使用量も大幅に削減される可能性がある [[102,104]]。これは、低スペックな環境やクラウド上でのコスト削減において大きな利点となる。
*   **並列処理の深化:** C++の並列処理ライブラリ（例：C++17の`std::thread`, Intel TBB）を活用し、ワールド生成、チック処理、ネットワークI/Oをより細かくスレッドに分散させる。例えば、`Chunk GC`のように、メインサーバースレッドの負荷を軽減する非同期処理を増やしていくことで、より多くのプレイヤーを捌けるスケーラブルなアーキテクチャへと進化できる [[255]]。
*   **データ指向アーキテクチャの導入:** MobのAIロジックやエンティティの更新処理など、CPUキャッシュの局部性を悪化させる散発的なメモリアクセスを改善するため、データ指向アーキテクチャの原則を適用する。例えば、同じ種類のエンティティ（例：全てのゾンビ）のデータをメモリ上に連続して配置する（AoS: Array of Structures -> SoA: Structure of Arraysへの変換）ことで、CPUのプリフェッチ機構が効率的に働き、性能を向上させることができる [[186,259]]。

| 項目 | cppfmの現状 | 改善後の理想像 | 主要なアーキテクチャ変更 |
| :--- | :--- | :--- | :--- |
| **スレッドモデル** | thread-per-connection  | Nettyイベントループ + サーバースレッドモデル [[10]] | スレッドプールと非同期タスクキューの導入 |
| **Chunk GC** | なし  | LRU + unload queue [[40]] | `ChunkCache`と`UnloadQueue`の実装 |
| **GC最適化** | C++ RAII  | Smart pointers, memory pooling [[237]] | `std::unique_ptr`/`std::shared_ptr`の徹底、専用アロケータの導入 |
| **監視** | なし  | Prometheus等連携可 [[92]] | Exporterエンドポイントの実装、メトリクスの公開 |
| **ログ** | stderr fprintf  | Log4j2 + async [[92]] | `spdlog`や`g3log`などの高性能なログライブラリの導入 |
| **ネイティブ実行** | Java .jar [[276]] | GraalVM Native Imageによるネイティブバイナリ [[102]] | CMake/Makefileの修正、ネイティブコンパイル環境の構築 |

まとめ而言って、`cpp-fabricmc`は、現在の状態では「遊びつくせるオープンワールド」を持っているものの、「本家サーバーと同等の機能を備えたゲーム環境」には程遠い。しかし、本レポートで示した網羅的な分析と、データ駆動型の世界生成、イベント駆動型の物理法則、永続化可能なエンティティ、一体化されたインベントリシステム、そして再現されたBrigadierコマンドシステムといった主要機能を段階的に実装し、C++ならではの性能とメモリ管理の強みを活かしたアーキテクチャ設計を追求することで、その可能性は大きく広がる。最終的な目標は、`cpp-fabricmc`を単なる「C++で書かれた軽量なserver.jarの代替」から、**「独自のアーキテクチャと性能特性を持つ、Minecraftサーバーの新たな選択肢」**へと昇華させることである。